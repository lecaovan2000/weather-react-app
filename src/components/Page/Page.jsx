import React, { Fragment } from 'react';

import Header from '../Header';
import Forecast from '../Forecast';
import CurrentDay from '../CurrentDay';
import Footer from '../footer/Footer';

const Page = () => {
    return (
        <Fragment>
            {/* <Header />
            <Forecast currentDay={<CurrentDay />} />
            <Footer /> */}
        </Fragment>
    );
};

export default Page;
